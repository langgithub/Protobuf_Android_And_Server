//protobuf的格式 只能为proto2 或者protp3 其他格式报错
syntax = "proto3";

//生成java类所在的包名
package com.lang.protobufdemo.entity ;

//登录请求结构体
message LoginRequest {
    string username = 1;
    string password = 2;
}

//登录响应结构体
message LoginResponse {
    int32 code = 1;
    string msg = 2;
}